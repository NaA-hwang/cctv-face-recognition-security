# ===============================================================================
# **Vercel 배포용: CCTV 용의자 식별 시스템 라이브러리**
# ===============================================================================
# 설치 명령어: pip install -r requirements.txt
# ===============================================================================

# Web Framework - 핵심 Flask 스택
Flask==2.3.3
Flask-CORS==4.0.0
Werkzeug==2.3.7

# ===============================================================================
# **Vercel 서버리스 환경 최적화 버전**
# ===============================================================================
# Computer Vision & AI - Vercel 환경에 맞게 최적화
opencv-python-headless==4.8.1.78  # GUI 없는 헤드리스 버전
numpy==1.24.3
Pillow==10.0.1

# 🚨 주의: 대용량 AI 모델 (InsightFace)은 서버리스 환경에서 제한이 있을 수 있음
# 실제 AI 기능이 필요한 경우 별도의 클라우드 서비스 고려 필요
# insightface==0.7.3
# onnxruntime==1.16.1

# Data Processing
pandas==2.0.3
sqlite3

# Utilities
python-dotenv==1.0.0
requests==2.31.0
tqdm==4.66.1

# Development & Testing
pytest==7.4.2
pytest-cov==4.1.0
black==23.9.1
flake8==6.1.0

# Optional: GPU Support (uncomment if using CUDA)
# onnxruntime-gpu==1.16.1
# torch==2.0.1+cu118
# torchvision==0.15.2+cu118