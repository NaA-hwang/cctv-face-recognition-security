# ===============================================================================
# **Vercel 배포용: CCTV 용의자 식별 시스템 라이브러리**
# ===============================================================================
# Python 3.12 호환성을 위한 최적화된 버전
# ===============================================================================

# Web Framework - 핵심 Flask 스택
Flask==3.0.0
Flask-CORS==4.0.0
Werkzeug==3.0.1

# ===============================================================================
# **Vercel 서버리스 환경 최적화 버전 (Python 3.12 호환)**
# ===============================================================================
# 수치 계산 - 최신 버전 (Python 3.12 호환)
numpy>=1.26.0

# 컴퓨터 비전 - 헤드리스 버전 (GUI 없음)
opencv-python-headless>=4.8.0

# 이미지 처리
Pillow>=10.0.0

# 웹 요청
requests>=2.31.0

# 🚨 참고: 대용량 AI 모델은 서버리스 환경에서 제한됨
# 실제 AI 기능이 필요한 경우 별도의 클라우드 AI 서비스 사용 권장
# insightface - 서버리스 환경에서 크기 제한으로 제외
# onnxruntime - 서버리스 환경에서 크기 제한으로 제외

# Data Processing
pandas==2.0.3
sqlite3

# Utilities
python-dotenv==1.0.0
requests==2.31.0
tqdm==4.66.1

# Development & Testing
pytest==7.4.2
pytest-cov==4.1.0
black==23.9.1
flake8==6.1.0

# Optional: GPU Support (uncomment if using CUDA)
# onnxruntime-gpu==1.16.1
# torch==2.0.1+cu118
# torchvision==0.15.2+cu118